# OpenAPI Source

This project fetches OpenAPI JSON directly from:

- `http://localhost:8000/docs-json`

Make sure your backend is running locally, then generate clients with:

```bash
pnpm generate:api
```
